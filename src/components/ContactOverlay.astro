<section class="contact" data-animate="contact">
  <div class="contact__inner">
    <p class="contact__label" data-animate="contact-label">Get in touch</p>
    <h2 class="contact__heading" data-animate="contact-heading">
      Let's work<br />together
    </h2>
    <div class="contact__links" data-animate="contact-links">
      <a href="https://github.com/Kcyle" target="_blank" rel="noopener" class="contact__link">GitHub</a>
      <a href="https://linkedin.com" target="_blank" rel="noopener" class="contact__link">LinkedIn</a>
      <a href="mailto:kyle@example.com" class="contact__link">Email</a>
    </div>
  </div>
</section>

<style>
  .contact {
    padding: 10rem var(--page-padding);
    display: flex;
    justify-content: center;
    text-align: center;
  }

  .contact__inner {
    max-width: 600px;
  }

  .contact__label {
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-text-muted);
    margin-bottom: 2rem;
  }

  .contact__heading {
    font-size: clamp(3rem, 8vw, 6rem);
    font-weight: 800;
    letter-spacing: -0.04em;
    line-height: 0.95;
    color: var(--color-text);
    margin-bottom: 3rem;
  }

  .contact__links {
    display: flex;
    justify-content: center;
    gap: 1rem;
  }

  .contact__link {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--color-text);
    padding: 0.75rem 1.75rem;
    border: 1px solid var(--color-border);
    border-radius: 100px;
    transition: all var(--transition-base);
  }

  .contact__link:hover {
    background: var(--color-text);
    color: var(--color-bg);
    border-color: var(--color-text);
  }

  @media (max-width: 768px) {
    .contact {
      padding: 6rem var(--page-padding);
    }

    .contact__links {
      flex-direction: column;
      align-items: center;
    }

    .contact__link {
      width: 100%;
      max-width: 200px;
      text-align: center;
    }
  }
</style>
